'use client';

// Temporary hook for match celebration (stub)
export default function useMatchFX() {
  const celebrate = () => {
    console.log('🎉 Match celebration triggered!');
    // Later we can add confetti, sound, and Lottie animation here
  };
  return { celebrate };
}

