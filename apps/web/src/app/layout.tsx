export default function RootLayout({children}:{children:React.ReactNode}){
  return <html><body style={{fontFamily:'system-ui, sans-serif', background:'#0b0b0e', color:'#fff'}}>{children}</body></html>
}
